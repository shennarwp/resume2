name: Daily image cleanup

on:
  push:
    branches: ['cleanup-ghcr']
  schedule:
    - cron: '30 1 * * *'
  workflow_dispatch:

jobs:
  cleanup:
    runs-on: ubuntu-latest
    permissions:
      packages: write
    steps:
      - uses: dataaxiom/ghcr-cleanup-action@v1
        with:
          package: resume
          use-regex: true
          skip-regex-checks: true
          exclude-tags: ^([^\s]+)?[0-9]{4}\.[0-9]{2}\.[0-9]{2}(-[^\s]+)?$
          keep-n-untagged: 10
          delete-partial-images: true
          delete-orphaned-images: true
          delete-ghost-images: true
          dry-run: true