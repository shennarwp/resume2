version: '3'
services:
  resume:
    image: ghcr.io/shennarwp/resume:nginx-latest
    container_name: resume
    restart: always
    expose:
      - 80
    networks:
      - nginx

networks:
  nginx:
    external: true
